name: vouch-check-pr


on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "vouch gate disabled for this template instance"

