rocs:
  layers:
    - name: core
      ref: "<repo:core/ontology-kernel@v0.2.0>"
    - name: company
      ref: "<repo:softwareco/ontology@main>"
    - name: repo
      path: "ontology/src"
  profiles:
    default: "repo-dev"
    guiding-circle:
      include_layers: ["core", "company"]
      exclude_layers: ["repo"]
      budget: 1800
    repo-dev:
      include_layers: ["core", "company", "repo"]
      budget: 2800

