---
summary: "Manual entry point for browsing the fork lane ontology surface."
read_when:
  - "When navigating lane-root ontology files by hand"
  - "When deciding where a lane-level concept should live"
---

# Ontology Index (lane root)

Start here when browsing manually.

- `ontology/manifest.yaml` — which layers apply
- `ontology/src/system4d.yaml` — lane-root System4D concepts
- `ontology/src/reference/concepts/` — lane-level concepts, only when needed
- `ontology/src/bridge/mapping.yaml` — concept-to-artifact mappings
