---
summary: "Guidance for adding lane-root ontology concepts."
read_when:
  - "When introducing a new fork-lane concept"
  - "When deciding whether a concept belongs in lane-root or a child repo"
---

# Lane Concepts

Add lane-root concepts here only when they truly apply across multiple child repos.
If a concept is specific to one fork, keep it in that child repo instead.
