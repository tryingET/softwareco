---
summary: "Placeholder for core docs snapshot (vendored or submodule)."
read_when:
  - "When creating/updating docs under docs/_core"
---

# Core Docs (`docs/_core/`)

This folder is the **Holding core snapshot** (or a git submodule) for this repo.

Preferred source: `ai-society/holdingco/holdingco-os` (`docs/core/**`, `governance/**`).

Recommended: vendor a snapshot first; migrate to submodule later if you need tight sync.
