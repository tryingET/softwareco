#!/usr/bin/env sh
# Package CI runs from monorepo root
# Note: scripts/rocs.sh is available at monorepo root
echo "This package's CI runs from the monorepo root." >&2
echo "Use: ../../scripts/rocs.sh for ROCS commands" >&2
exit 0
