#!/usr/bin/env sh
# ROCS runs from monorepo root
# This script is a placeholder - actual ROCS calls should be from monorepo root
echo "ROCS commands should be run from the monorepo root." >&2
echo "Use: ../../scripts/rocs.sh <args>" >&2
exit 1
