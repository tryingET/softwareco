---
summary: "Org context snapshot for this monorepo root."
read_when:
  - "When workspace-wide choices depend on org constraints or governance"
---

# Org Context (`docs/org_context/`)

This folder carries the organization context the monorepo root should preserve.

Minimum compact baseline:
- `org-summary.md`

Rich profile additions:
- mission / purpose / vision / strategic objectives / governance context
