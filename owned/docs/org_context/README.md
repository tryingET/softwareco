---
summary: "Org context for this project (snapshot or submodule)."
read_when:
  - "When the project needs org constraints/policy context"
---

# Org Context (`docs/org_context/`)

This folder captures the relevant org handbook context for this project.

Recommended (start minimal):
- Add `org-summary.md` with the key constraints and links.

Optional (later):
- Make this a git submodule pointing to `<org>/org-handbook` if you want tight sync.

