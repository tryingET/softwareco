---
summary: "Evidence and observed gaps supporting a successor recurring Softwareco portfolio CTO mandate."
read_when:
  - "Reviewing whether Decision 74 proves recurring CTO operation or only a finite canary."
type: "evidence"
status: "decision_pending"
date: "2026-07-25"
decision_id: 77
predecessor_decision_id: 74
governance_task_id: 4205
---

# Evidence — recurring Softwareco portfolio CTO

## Decision 74 facts preserved

Fresh AK and repository readback establishes:

- Decision `74` remains accepted historical law for its finite mandate.
- Applied human receipt `8870` records `delegated_active -> complete` at `2026-07-25T13:19:10.466752718Z`.
- `SF3` remains the required active strategic root but carries exact terminal Decision 74 detail; this grants no CTO authority.
- Controller task `4182` is `done` with `mandate_complete_handed_back`.
- Corrective task `4184`, coordinator task `4186`, owner task `3425`, terminal task `4191`, and topology-correction task `4199` are done.
- First-wave portfolio outcome evidence is `5176`; corrective D2E evidence is `5177`; terminal reconciliation/correction evidence is `5193`/`5194`.
- DesignMD Foundry owner commits `3c65773` and `33ff05b` are present and its worktree was clean at closeout.
- `./scripts/check-cto-operator-surface.sh --require-terminal` passes and `--require-active` fails closed.
- `ak direction check --repo /home/tryinget/ai-society/softwareco` passes with zero issues.

These facts prove the canary and its lawful handback. They do not authorize a successor.

## What the first dogfood proved

Decision 74 demonstrated the full federal chain once:

```text
proved-empty sensing
-> proposal-only ranking
-> direct Product/Project acceptance
-> portfolio admission
-> exact owner task
-> owner implementation and validation
-> direct terminal acceptance
-> portfolio release
-> outcome evidence
```

The corrected `/cto` gate also proved that empty portfolio membership is a valid sensing result but never consent.

## What remains unproved

The Decision 74 evidence explicitly says the first completed wave is not proof of recurring operation. No current evidence shows:

- a second portfolio outcome in another owner repository;
- fresh-session reconstruction after an existing completed wave;
- an AK-native thesis revision that can be reconstructed without the prior session;
- post-outcome re-entry and another sensing cycle;
- controller continuity across disposable worker sessions;
- objective completion without mandate termination.

## Current implementation coupling

The tracked operator surface is still instance-specific:

- `.pi/prompts/cto.md` names Decision `74`, acceptance receipt `8818`, controller task `4182`, fixed acceptance/expiry values, and Decision 74 terminal concerns;
- `.pi/modes/softwareco-cto.json` embeds the Decision 74 contract;
- `scripts/check-cto-operator-surface.sh` verifies fixed Decision 74 receipts, timestamps, controller, first wave, and terminal evidence;
- the historical thesis document mixes proposal-stage facts with later lifecycle evidence deliberately and is not a live thesis store.

This coupling correctly prevents continued Decision 74 operation. It must be generalized only after a successor decision is accepted.

## Operator correction

After the canary was terminally reconciled, the operator rejected the interpretation that the broader objective was complete and confirmed this corrected target:

> Establish and dogfood a recurring Softwareco CTO operating loop that can restart from durable state, maintain a portfolio thesis, select and execute successive owner-native waves, and verify at least one additional independent portfolio outcome—not merely pass a single canary.

This correction is direction input, not itself a delegation or decision acceptance.

## Independent pre-RFC exploration

Two read-only review tracks examined the successor shape:

- authority review `dispatch-1784987618874` concluded that Decision 74 must remain terminal history and recommended a new standing mandate with bounded operating epochs;
- runtime review `dispatch-1784987618876` concluded that recurrence requires independent `--no-session` sensing, owner-native successive-wave evidence, and post-outcome re-entry from durable AK/source-owner facts.

Both reviews rejected reopening Decision 74 or treating task `4205`, Pi mode activation, a strategic-root state, or session memory as authority.

## Candidate follow-up outcomes

The Decision 74 thesis deferred two potentially valuable independent candidates:

1. Context Packer bounded excerpts for oversized explicit Markdown seeds—high leverage, but previously blocked by a divergent/dirty owner baseline.
2. Pi Server explicit replay-fingerprint schema—useful auditability, but previously blocked by dirty-tree, public-contract, and Node-version coordination.

These are evidence-backed candidates, not admitted work or backlog commitments. Fresh owner-repo sensing and direct owner acceptance are still required after successor activation.

## Constraints carried forward

- Maximum two admitted waves and six outstanding admitted owner tasks.
- A second concurrent wave requires direct human checkpoint.
- Owners originate outcome, capacity, displacement, scope, objection, terminal, and release receipts.
- Portfolio wrappers never mutate owner task lifecycle.
- No release, publication, external effect, architecture acceptance, owner appointment, product lifecycle decision, or sensitive exception without the reserved human/owner gate.
- Decision 68's broader template-propagation freeze remains unchanged.
- The unrelated pre-existing modification at `owned/docs/project/repo-capability-map.md` is outside this work.
